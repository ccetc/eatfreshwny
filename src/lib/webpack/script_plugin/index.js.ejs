import reframe from 'reframe'
<% for(var i = 0; i < components.length; i++) {-%>
import <%= components[i] %> from '<%= root %>/src/app/components/<%= components[i] %>'
<% } -%>
import index from '<%= root %>/src/app'

const reducers = [
  ...Object.values(reframe),
<% for(var i = 0; i < components.length; i++) {-%>
  <%= components[i] %>,
<% } -%>
]

index(reducers)
